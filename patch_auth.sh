sed -i 's/val userObj = sessionData?.get("user") as? Map<\*, \*>/val accessToken = sessionData?.get("access_token") as? String\n                        if (accessToken != null) {\n                            currentAccessToken = accessToken\n                            Log.d(TAG, "Successfully acquired and stored JWT access_token")\n                        }\n                        val userObj = sessionData?.get("user") as? Map<\*, \*>/g' app/src/main/java/com/example/data/SupabaseClient.kt
